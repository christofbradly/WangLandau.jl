name: Benchmarks (4 threads)

on:
  pull_request:
    branches: [main]

permissions:
  pull-requests: write

jobs:
  four-threads:
    runs-on: ubuntu-latest
    env:
      JULIA_NUM_THREADS: "4"

    steps:
      - uses: actions/checkout@v4
      - uses: MilesCranmer/AirspeedVelocity.jl@action-v1
        with:
          julia-version: "1.11"
          script: benchmark/benchmarks.jl
